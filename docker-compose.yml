services:
  palmtop:
    build: .
    volumes:
      - ./config.toml:/app/config.toml:ro
      - palmtop-data:/app/data
    env_file:
      - path: .env
        required: false
    ports:
      - "8000:8000"
    restart: unless-stopped

volumes:
  palmtop-data:

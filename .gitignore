# Persona and runtime
config.toml
.env
.env.keys          # dotenvx decryption keys (NEVER commit — add to CI secrets)
*.gguf
models/
data/
# .env.vault is safe to commit (encrypted with AES-256-GCM)

# Python
__pycache__/
*.py[cod]
build/
dist/
wheels/
*.egg-info
.venv/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Claude Code
.claude/

# Lock file (optional — remove this line if you want to pin deps)
# uv.lock
